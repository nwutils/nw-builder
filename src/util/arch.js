/**
 * Get user's computer architecture
 *
 * @param  {string}                  arch  Node's process.arch
 * @return {"ia32"| "x64" | "arm64"}       NW.js supported architectures
 */
export const getArch = (arch) => {
  return arch;
};
